# Minerva Environment Variables
# Copy this file to .env and fill in your actual values

# OpenAI API key (required for OpenAI provider)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# Ollama base URL (optional, defaults to http://localhost:11434)
# Only needed if Ollama is running on a different host/port
# OLLAMA_HOST=http://localhost:11434

# Webhook secret for GitHub webhook signature validation
# Generate with: openssl rand -hex 32
# Only needed if using github-webhook-orchestrator
WEBHOOK_SECRET=your-webhook-secret-here

# GitHub personal access token (optional)
# Only needed for private repositories or higher API rate limits
# GITHUB_TOKEN=ghp_your-token-here

# Minerva API key for remote HTTP server authentication
# Generate with: openssl rand -hex 32
# Only needed for remote team deployment (minerva serve-http)
# MINERVA_API_KEY=your-api-key-here
