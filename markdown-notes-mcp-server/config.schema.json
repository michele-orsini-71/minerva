{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MCP Server Configuration",
  "description": "Configuration schema for the markdown notes MCP server. Note: AI provider configuration (embedding models, LLM models) is stored in collection metadata and does not need to be specified here.",
  "type": "object",
  "required": [
    "chromadb_path",
    "default_max_results"
  ],
  "properties": {
    "chromadb_path": {
      "type": "string",
      "description": "Absolute path to the ChromaDB persistent storage directory",
      "minLength": 1,
      "pattern": "^/",
      "examples": [
        "/Users/username/my-code/search-markdown-notes/chromadb_data",
        "/home/user/chromadb_data"
      ]
    },
    "default_max_results": {
      "type": "integer",
      "description": "Default maximum number of search results to return",
      "minimum": 1,
      "maximum": 100,
      "default": 5
    }
  },
  "additionalProperties": false
}
